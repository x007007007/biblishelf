machine:
  post:
    - pyenv global 3.5.2 3.6.0
dependencies:
  pre:
    - pip install tox
test:
  pre:
    - tox
deployment:
  production:
    branch: master
    commands:
      - fab deploy
  feature:
    branch: /feature_.*/
    commands:
      - ./deploy_feature.sh
